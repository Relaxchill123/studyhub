пять контрольных сценариев: пустой список, добавление корректной задачи, отказ для пустого названия, просмотр списка, завершение существующей задачи.

[] | show_menu() -> 2 -> show_tasks(tasks) -> печатает Список задач еще пуст + show_menu()

вход: 2. Показать задачи, ожидаемое изменение списка: нет, точный пользовательский результат: Список задач еще пуст + show_menu()

Повторить Python, 2 | show_menu() -> 1. Добавить задачу -> add_task(tasks) -> Пользователь вводит задачу: "Повторить Python" -> if not noramalize_title(title) -> возвращает title.strip() (не False) -> title = noramalize_title(title) -> возвращает title.strip() -> пользователь вводит приоритет: "2" -> if is_normalizate_priority(priority) возвращает True -> tasks.append({...}) -> Печатает: "Задача добавлена в tasks"

вход: 1; Повторить Python; 2, ожидаемое изменение списка: да, точный пользовательский результат: Задача добавлена в tasks  + show_menu()

"" | | show_menu() -> 1. Добавить задачу -> add_task(tasks) -> Пользователь вводит задачу: "Повторить Python" -> if not noramalize_title(title) -> возвращает False ->  Печатает: Название не может быть пустым -> continue -> Пользователь должен ввести задачу заново

вход: 1; "", ожидаемое изменение списка: Нет, точный пользовательский результат: Название не может быть пустым + show_menu() 

[{...}, {...}] | show_menu() -> 2. Показать задачи -> show_tasks(tasks) -> Печатает строки

вход: 2; "", ожидаемое изменение списка: Нет, точный пользовательский результат: ID: ... Задача: ... Приоритет: ... Статус: ... + show_menu()

завершение существующей задачи | show_menu() -> 4. Отметить выполненную задачу ->
if tasks -> True - пользователь вводит ID задачи для завершения ->
    if task_id: True - 
        if введенный_ID not in get_tasks_ids(tasks) -> get_tasks_ids(tasks) -> 
                False - печатает: Переданной задачи нет в списке, просмотреть список задач? Y/N -> пользователь вводит Y -> show_tasks(tasks) -> mark_done_tasks() 
                    True - проверяем not is_done ->
                    True - is_done = True -> печатает: Отмечена как выполненная
                    False - печатает: Задача уже выполнена
    False - печатает: ID задачи не введен -> continue
False: печатает: Список задач пуст        

вход: 4; 1, ожидаемое изменение списка: Да, точный пользовательский результат: Отмечена как выполненная + show_menu()