ядро готово, persistent слой и CLI ждут реализации.

Не разобрался в занятии 42 в практике пункты:  5.1/2/3:

В app/main.py объявите build_service(storage=None).
Если storage передан, верните PlannerService с ним.
Если storage не передан, на этом этапе допустимо использовать MemoryStorage.

Не разобрался в занятии 43 в практике пункты:  3.3:

Если отдельный элемент не создаёт корректную Task, также поднимайте StorageError.